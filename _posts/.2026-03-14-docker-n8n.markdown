---
layout: post
title: "Docker models and n8n workflows"
date: 2026-03-14 21:00:09 -0300
categories: docker ai model n8n
---
# Use Docker AI model in n8n

## Run n8n

```bash
docker run -it --rm --name n8n -p 5678:5678 -v n8n_data:/home/node/.n8n docker.n8n.io/n8nio/n8n
```

## 

```bash
curl -sL -X GET "https://api.telegram.org/bot${BOT_TOKEN}/${path}?${parameters}"
```

## Write OpenAI client in Python

```python
from openai import OpenAI

client = OpenAI(
  base_url="https://your-custom-endpoint.com/v1",
  api_key="your-api-key"
)

response = client.chat.completions.create(
  model="your-model-name",
  messages=[{"role": "user", "content": "Hello!"}]
)
```

## 

```bash
npx localtunnel --port 12434
```
